server.port=8080

# Database Configuration (MariaDB)
spring.datasource.url=jdbc:mariadb://localhost:3307/book_store?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# JWT Configuration
jwt.secret=dionStoreSecretKeyForJWTAuthentication2026LuxuryBoarding

# Session tracking for cookies to work properly across ports during development
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.secure=true

# Mail Configuration (Disabled by default, update and uncomment to enable)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hungha.pn213@gmail.com
spring.mail.password=frnqmnaivilypggn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Gemini API
gemini.api.key=
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent
